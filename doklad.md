# Доклад за миграция към микросървиси (Baraba)

Дата: 19 декември 2025 г.
Статус: **Завършена стъпка 4 от 5**

## Какво е направено днес

Успешно продължихме процеса на разделяне на монолита `baraba` на микросървиси.

1.  **Библиотека `baraba_shared`**:
    *   Създадена е обща библиотека (Shared Kernel), която съдържа модели (`User`, `UserGroup`, `Company`, `Counterpart`, `Journal`), конфигурация за база данни (`db/config.nim`) и помощни функции (`security`, `json_utils`, `i18n`).
    *   Това позволява на всички микросървиси да споделят код без дублиране.

2.  **Scanner Service (Порт 5001)** - **Завършен**:
    *   Изваден е модулът за AI сканиране в самостоятелен проект `scanner_service`.
    *   Основният монолит вече не обработва сканирането, а прави HTTP заявка към този сървис.

3.  **Identity Service (Порт 5002)** - **Завършен**:
    *   Създадена е структурата на `identity_service`.
    *   Този сървис съдържа логиката за `Login`, `Register`, `Me` и цялостното управление на потребители (`/api/users/*` и `/api/user-groups/*`).
    *   Монолитът е настроен да препраща (`proxy`) всички заявки за автентикация и управление на потребители към новия сървис.
    *   Преместени са `routes/user_routes.nim` и `routes/user_group_routes.nim` в `identity_service`.

4.  **VIES Service (Порт 5003)** - **Завършен**:
    *   Създаден е `vies_service` като отделен микросървис за валидация на VAT номера чрез EU VIES система.
    *   Включва ендпойнт `/api/vies/validate/{vatNumber}`.
    *   Монолитът е настроен да препраща всички VIES заявки към този микросървис.

5.  **VAT Service (Порт 5004)** - **Завършен**:
    *   Създаден е `vat_service` като отделен микросървис за генериране на VAT файлове за НАП.
    *   Включва сложната логика за генериране на `POKUPKI.TXT`, `PRODAGBI.TXT` и `DEKLAR.TXT`.
    *   Монолитът е настроен да препраща всички VAT генерационни заявки към този микросървис.

6.  **Docker Compose** - **Завършен**:
    *   Създаден е `docker-compose.yml` файл, който вдига и петте сървиса + Postgres + Frontend базата автоматично.
    *   Добавени са Dockerfile файлове за всички услуги.
    *   Създадена е документация в `DOCKER.md` за лесно стартиране и управление.
    *   **Frontend Impact**: Анализирано е въздействието върху frontend-а - почти не се нуждае от промени! (виж `FRONTEND_IMPACT.md`)

## Frontend Въздействие

**🎉 Добра новина: Frontend-ът вече работи перфектно с микросървисите!**

Благодарение на добрата архитектура:
- **Vite proxy** пренасочва всички `/api` заявки към монолит (порт 5000)
- **Монолитът работи като API Gateway** и пренасочва към правилните микросървиси
- **JWT autentикацията** работи прозрачно през Identity Service
- **Всички API клиентски функции** работят без промени

### Какво работи автоматично:
- ✅ Login/Register → Identity Service (5002)
- ✅ User Management → Identity Service (5002)  
- ✅ VAT Validation → VIES Service (5003)
- ✅ VAT Generation → VAT Service (5004)
- ✅ Document Scanning → Scanner Service (5001)
- ✅ Всички останали → Монолит (5000)

### Достъп до приложението:
- **Frontend**: http://localhost:5173
- **API Gateway**: http://localhost:5000
- **Всички микросървиси** се достъпват през API Gateway

## Инструкции за тестване (за екипа утре)

**🚀 Препоръчителен метод: Docker Compose (една команда!)**

#### 1. Стартиране на всички услуги (включително frontend):
```bash
docker-compose up -d
```

#### 2. Инициализация на базата данни:
```bash
docker-compose exec baraba_service ./bin/migrate
```

#### 3. Проверка на статуса:
```bash
docker-compose ps
```

#### 4. Достъп до приложението:
Отворете в браузър: **http://localhost:5173**

### Ръчно стартиране (за development)
За да тествате системата в текущото ѝ състояние, трябва да стартирате 6 процеса едновременно:

#### 1. Стартиране на Scanner Service
В отделен терминал:
```bash
cd scanner_service
nimble build -y
./scanner_service
```
*Очаква се да слуша на порт 5001.*

#### 2. Стартиране на Identity Service
В отделен терминал:
```bash
cd identity_service
nimble build -y
./identity_service
```
*Очаква се да слуша на порт 5002.*

#### 3. Стартиране на VIES Service
В отделен терминал:
```bash
cd vies_service
nimble build -y
./vies_service
```
*Очаква се да слуша на порт 5003.*

#### 4. Стартиране на VAT Service
В отделен терминал:
```bash
cd vat_service
nimble build -y
./vat_service
```
*Очаква се да слуша на порт 5004.*

#### 5. Стартиране на Монолита (Baraba)
В отделен терминал:
```bash
# В главната директория
nimble build -y
./baraba
```
*Очаква се да слуша на порт 5000 (или 8080).*

### Метод 2: Docker Compose (Препоръчително)

#### 1. Стартиране на всички услуги:
```bash
docker-compose up -d
```

#### 2. Инициализация на базата данни:
```bash
docker-compose exec baraba_service ./bin/migrate
```

#### 3. Проверка на статуса:
```bash
docker-compose ps
```

### Сценарии за тестване

1.  **Логин (Login):**
    *   Опитайте да влезете в системата през Frontend-а.
    *   **Очакван резултат:** Монолитът ще приеме заявката, ще я прати към Identity Service (5002), който ще провери паролата в базата и ще върне Token. Всичко трябва да работи прозрачно за потребителя.

2.  **Регистрация и управление на потребители:**
    *   Опитайте да създадете, редактирате или изтриете потребители.
    *   **Очакван резултат:** Всички операции ще бъдат препратени към Identity Service (5002).

3.  **VIES валидация:**
    *   Опитайте да валидирате VAT номер.
    *   **Очакван резултат:** Заявката ще бъде препратена към VIES Service (5003).

4.  **VAT генерация:**
    *   Опитайте да генерирате VAT файлове за даден период.
    *   **Очакван резултат:** Заявката ще бъде препратена към VAT Service (5004).

5.  **Сканиране на фактура:**
    *   Отидете на "AI Скенер" и качете PDF (дори празен/тестов).
    *   **Очакван резултат:** Монолитът ще прати заявка към Scanner Service (5001), който ще върне (mock) резултат.

## Следващи стъпки (за довършване)

1.  **Подобряване на комуникацията:**
    *   В момента всички услуги споделят една база данни (Shared Database pattern).
    *   В бъдеще може да се премине към API-first подход с отделни бази данни за всеки микросървис.

2.  **Мониторинг и логване:**
    *   Да се добави централизирано логване за всички микросървиси.
    *   Да се настрои monitoring (например, Prometheus + Grafana).

3.  **Load Balancer:**
    *   В производствена среда да се добави reverse proxy/load balancer (nginx) пред микросървисите.

4.  **Допълнителни оптимизации:**
    *   Да се оптимизират междусървисните комуникации.
    *   Да се добави retry логика при грешки в комуникацията.

Успех с тестовете! Системата е стабилна и готова за финалния етап от миграцията.
